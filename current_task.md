1. FIX 1: при нажатии кнопки пользователь не видит, что она нажата и действия начались. необходимо заблокировать кнопку для дальнейшийх нажатий, показать пользователю, что идёт процесс работы над его запросом - DONE
2. FIX 2: необходимо проверить логику рассчёта кратчайшего расстояния. сейчас агент не справляется с корректным рассчётом при маршруте более чем с 3 точками. Нужно сделать так, чтобы он корректно считал оптимальный маршрут при задаче до 5 включительно населенных пунктов. Если пунктов больше 5 - агент должен взять только 5 первых городов в запросе пользователя и честно предупредить, что для большего числа пунктов выгрузки его мозгов не хватает - DONE
3. FIX 3: Алгоритм нахождения оптимальной дистанции работает правильно, но с одним ньюансом - он берет в качестве города отправления первый из списка. Поэтому, если рассчитан маршрут для 3 и более точек и пользователь явно не указал город отправления, то необходимо добавить в промпт вывода информацию, что "в качестве города отправления использован первый город из списка - ***" - DONE
4. FEATURE 1: Реализовать дополнительный MCP сервер для работы с api pecom.ru. Добавить агенту способность получать стоимость перевозки ООО "ПЭК" - DONE

5. FEATURE 2: реализовать RAG. 
## Задача: Индексация документов перевозчиков (Day 21)

### Цель
Создать локальный индекс для поиска по документам перевозчиков (ПЭК, СДЭК, Почта России) с эмбеддингами.

### Что уже есть
- Папка `data/carriers/` с текстовыми файлами (pecom.txt, cdek.txt, post_russia.txt)

### Что нужно сделать (один скрипт `indexer.py`)

#### 1. Загрузка документов
- Прочитать все .txt файлы из `data/carriers/`
- Сохранить метаданные: имя файла, путь

#### 2. Чанкинг (разбиение)
Сделать **две стратегии** для сравнения:

| Стратегия | Параметры |
|-----------|-----------|
| Фиксированный размер | chunk_size=500, overlap=50 |
| По предложениям | split по ". " "!\n" "?\n" |

#### 3. Эмбеддинги
- Использовать `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`
- Модель легковесная, работает на CPU
- Превратить каждый чанк в вектор

#### 4. Сохранение индекса
- **FAISS** — для быстрого поиска векторов
- **SQLite** — для хранения метаданных (текст чанка, имя файла, стратегия)

#### 5. Поиск
Функция `search(query, strategy="fixed", top_k=3)`:
- Превращает вопрос пользователя в вектор
- Ищет ближайшие чанки в FAISS
- Возвращает текст чанков + метаданные

#### 6. Сравнение стратегий
Вывести таблицу:
| Стратегия | Кол-во чанков | Средний размер | Время индексации |
|-----------+---------------+----------------+------------------|
| fixed | 45 | 480 символов | 2.3 сек |
| sentence | 28 | 320 символов | 1.8 сек |

text

### Запуск
```bash
python indexer.py
Ожидаемый результат
Создан файл faiss_index (бинарный индекс)

Создан файл metadata.db (SQLite)

В консоли вывод сравнения стратегий

При поиске "стоимость доставки до 50 кг" находятся чанки из pecom.txt

Зависимости (установи сам)
bash
pip install sentence-transformers faiss-cpu langchain sqlite3
Тестовый запрос для проверки
После индексации запусти поиск по фразе "сколько стоит отправить груз 30 кг из Москвы в Питер"

Должны найтись чанки из cdek.txt или pecom.txt.

Важно
Всё работает локально, без API

Код должен быть самодостаточным (один файл)

Добавь комментарии, чтобы было понятно что делает каждая функция